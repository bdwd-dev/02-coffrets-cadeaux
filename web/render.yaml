services:
  - type: web
    name: 02-coffrets-cadeaux-web
    env: static
    buildCommand: echo "No build needed"
    staticPublishPath: ./
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
